
<?php
include_once 'directorio/file';   

require_once 'file';    '---> Siempre se usa para incluir clases '
cookie: vinculado a un usuario y a una sesion. Se guarda en un direcctorio, si se accede al mismo archivo desde otrra ruta que no pase por el directorio donde esta la cookie esta no recibira la informacion.
sesion: un solo cliente, ligado a un navegador, (chrome, firefox independiente)
httpRequest: 
